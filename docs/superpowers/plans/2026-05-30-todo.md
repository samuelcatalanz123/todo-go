# Lista de tareas (SQLite) — Plan

**Goal:** App web en Go con tareas guardadas en SQLite (CRUD).
**Tech:** Go (database/sql, html/template, go:embed) + modernc.org/sqlite (Go puro).
Módulo: `github.com/samuelcatalanz123/todo-go`. Commits autoría Samuel.

## Tareas
1. **store (con prueba):** `internal/store/store.go` (Task, New+migración, Add, List, Toggle, Delete, Close) + `store_test.go` (CRUD con BD temporal).
2. **web:** `internal/web/handler.go` (GET /, POST /add, POST /toggle/{id}, POST /delete/{id}) + `templates/index.html` + `static/style.css`.
3. **main.go:** abre el store (DB_PATH) y arranca el servidor (:8080/PORT).
4. **README + .gitignore (ignora *.db).**
5. Verificar build/vet/test; arrancar y probar; commits.

Self-review: cobertura del spec ✔ · sin placeholders ✔ · tipos consistentes
(Task, Store, New, Add, List, Toggle, Delete) ✔.
